name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - id: checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Set up Python 3.9
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.9"
      - name: Verify Python 3.9
        env:
          SUPER_CODEX_SENSITIVE_TERM_DIGESTS: ${{ secrets.SUPER_CODEX_SENSITIVE_TERM_DIGESTS }}
        run: |
          python scripts/install_gitleaks.py
          python scripts/check_sensitive_data.py --history
          python scripts/run_gitleaks.py --history
          python -m unittest discover -s tests -v
          python -m compileall -q src tests scripts
          python -m pip install .
          sc --version
          super-codex --version
      - id: python313
        name: Set up Python 3.13
        if: ${{ !cancelled() && steps.checkout.outcome == 'success' }}
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.13"
      - name: Verify Python 3.13
        if: ${{ !cancelled() && steps.python313.outcome == 'success' }}
        run: |
          python -m unittest discover -s tests -v
          python -m compileall -q src tests scripts
          python -m pip install .
          sc --version
          super-codex --version
